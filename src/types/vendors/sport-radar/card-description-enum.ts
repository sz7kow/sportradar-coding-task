export enum CardDescriptionEnum {
  PRE_MATCH = 'pre_match',
  HALF_TIME = 'half_time',
  POST_MATCH = 'post_match',
  PLAYER_ON_BENCH = 'player_on_bench',
  FIRST_HALF = 'first_half',
  SECOND_HALF = 'second_half',
  DURING_PENALTY_SHOOTOUT = 'during_penalty_shootout',
}
